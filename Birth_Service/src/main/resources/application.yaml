spring:
  datasource:
    url: jdbc:mysql://localhost:3306/reviewdb # 이코드는 host주소부분을 멘토님 주소로 바꾸면 될 것 같습니다
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: 'root'
    password: '0000'
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true

